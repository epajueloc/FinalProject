<h1>Postings</h1>

<button><%= link_to 'New Posting', new_user_posting_path(current_user,@posting)%></button>

<% @postings.each do |posting|%>
<h4><strong> <%= link_to posting.title, user_posting_path(current_user, posting) %></strong></h4>
<p><%= posting.description %></p>
<% end %>